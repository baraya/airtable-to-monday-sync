# Node modules and logs
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# VSCode settings (optional)
.vscode/

# Local env config (if testing locally)
.env

# Synced record cache file (will be downloaded/uploaded by the workflow)
synced.json

# OS / editor noise
.DS_Store
Thumbs.db
